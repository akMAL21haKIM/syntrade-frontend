import TradePage from "./trade";

const App = () => {
  // Check if user is authenticated or not
  // If user is authenticated, return Trade page
  // Else, return Login page

  return <TradePage />;
};

export default App;
